// A host planning their own network campaigns, inside the host shell. Same
// content calendar as /advertiser/calendar; its links are base-path aware (see
// lib/useBasePath), so they stay under /host.
export { default } from '@/app/advertiser/calendar/page'
