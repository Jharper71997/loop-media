// Host-side entry to the advertise buy flow. Same page as /advertiser/browse,
// rendered under the host shell; the client components derive their /host/advertise
// links from the URL (see lib/useBasePath).
export { default } from '@/app/advertiser/browse/page'
