// A host managing one of their own network campaigns, inside the host shell.
// Same detail page as /advertiser/campaigns/[id]; its controls + back link are
// base-path aware (see lib/useBasePath), so they stay under /host.
export { default } from '@/app/advertiser/campaigns/[id]/page'
